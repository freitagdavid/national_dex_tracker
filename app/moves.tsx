import { PlaceholderScreen } from "../src/components/navigation";

export default function MovesScreen() {
	return (
		<PlaceholderScreen
			title="Move browser"
			description="Search and browse Pokémon moves, PP, types, and learnsets. Hook this screen up to your data when you are ready."
		/>
	);
}
