import { PlaceholderScreen } from "@/components/navigation/PlaceholderScreen";

export default function LocationsScreen() {
	return (
		<PlaceholderScreen
			title="Locations"
			description="Routes, towns, and where to find species — tie this to your encounter or location data later."
		/>
	);
}
