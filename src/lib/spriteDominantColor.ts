/**
 * Native: no canvas; list/modal theming falls back to type colors in `usePokemonListImageTheme`.
 * @see spriteDominantColor.web.ts
 */
export async function sampleDominantCardColor(
	_imageUrl: string,
): Promise<string | null> {
	return null;
}
