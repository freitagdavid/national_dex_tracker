[build]
  command = "npm ci && npx expo export -p web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: client-side app has no server routes; deep links and refresh should serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
